import 'dart:ui';

/// 竖图宽度
final VERTICAL_IMAGE_WIDTH =
    (window.physicalSize.width / window.devicePixelRatio - 10) / 4;

/// 竖图高度
final VERTICAL_IMAGE_HEIGHT =
    (window.physicalSize.width / window.devicePixelRatio - 10) / 4 * 1.5;

/// 横图宽度
final HORIZONTAL_IMAGE_WIDTH =
    (window.physicalSize.width / window.devicePixelRatio - 40) / 2;

/// 横图高度
final HORIZONTAL_IMAGE_HEIGHT =
    (window.physicalSize.width / window.devicePixelRatio - 40) / 3;
